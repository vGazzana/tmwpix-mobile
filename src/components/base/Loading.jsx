import {View, Text} from 'react-native';
import React from 'react';

const Loading = () => {
  return (
    <View className="top-0 left-0 flex h-screen w-screen items-center justify-center bg-black/20">
      <Text>Loading...</Text>
    </View>
  );
};

export default Loading;
